fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
# neomodel 6.x stores adb.driver in a ContextVar, so every ASGI request task
# sees driver=None and opens a new AsyncDriver + bolt socket that is never
# closed — fd leak → EMFILE. Stay on 5.x (process-wide driver) until the app
# is migrated to neomodel 6's per-context connection model.
neomodel>=5.4.0,<6
redis>=5.0.0
celery>=5.4.0
watchdog>=4.0.0
GitPython>=3.1.40
fastapi-mcp>=0.3.0
httpx>=0.27.0
python-multipart>=0.0.9
pytest>=8.2.0
pytest-asyncio>=0.23.0
PyYAML>=6.0
rapidfuzz>=3.0
croniter>=2.0.0
PyJWT>=2.8
cryptography>=42
